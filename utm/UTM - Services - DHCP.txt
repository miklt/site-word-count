https://docs.blockbit.com/display/RC/UTM+-+Services+-+DHCP
264
['', 'UTM - Services - DHCP', '']
['', 'Gerenciador do protocolo DHCP (RFC2131) que atua sobre camada de aplicação nas portas padrões do DHCP 67/68 UDP. Numa rede de arquitetura TCP/IP, todo computador tem que possuir um endereço IP distinto. O DHCP (Dynamic Host Configuration Protocol) é o protocolo que provê um meio para alocar estes endereços dinamicamente.O serviço DHCP é um gerenciador do protocolo DHCP que atua sobre camada de aplicação nas portas padrões do DHCP 67/68 UDP. Numa rede de arquitetura TCP/IP, todo computador tem que possuir um endereço IP distinto.O DHCP é responsável por distribuir os endereçamentos IP e configurações de rede para seu ambiente corporativo. É uma eficiente solução já que, por meio dele, o servidor BLOCKBIT UTM distribui endereços IP na medida em que os dispositivos da rede solicitam conexão. É importante frisar que, além do endereço IP, também atribui outros parâmetros, tais como: nome do host, DNS, rota default.Recursos do DHCP:Distribuição de endereços IP por device / por servidor:Ethernet;Vlan;Mac Vlan (device de endereçamento virtual);Distribuição de endereços IP por rede / sub-rede;Políticas para distribuição de endereço IP;Suporte a autenticação por servidor Radius.Modelos:Distribuição por faixa (range);Distribuição de endereços estáticos. (reserva de endereço de IP por filtro MAC).Filtros:MAC;Host.Parâmetros:Gateway;Sufixo DNS;Múltiplos DNS;Múltiplos Wins;TTL - Tempo de renovação (tempo de vida).Para acessar a tela do\xa0DHCP, selecione a opção\xa0conforme demonstrado na imagem abaixo:Services - DHCPA seguinte tela será exibida:DHCPA seguir, explicaremos as demais abas da interface DHCP em detalhe:Server;Relay;Leases.', '']
