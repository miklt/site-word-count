https://docs.blockbit.com/pages/viewpage.action?pageId=20541588
401
['', 'Servers - Integração de domínio e sincronismo Windows AD/LDAP', '']
['', 'A autenticação integrada suporta autenticação Windows AD e/ou LDAP e é baseada no sincronismo da base de usuários. A autenticação baseada no sincronismo Windows AD requer que o servidor BLOCKBIT UTM seja integrado ao domínio. Por fim, a autenticação baseada no sincronismo LDAP autêntica diretamente na base LDAP, não necessitando integração ao domínio.Para o processo de integração de domínio é necessário certificar que o servidor esteja configurado adequadamente para o ingresso no controlador de domínio do servidor Windows.Inicialmente, acesse\xa0Network - Settings.Settings - Network SettingsNa aba Settings, certificar-se que o endereço do campo DNS server 1\xa0esteja configurado para o endereço IP do controlador de domínio do seu servidor Windows, onde pretende-se aplicar o sincronismo de usuários.Podemos configurar o item de autenticação de usuários.De volta em Authentication - Servers.Authentication - ServersPara a autenticação de usuários temos as abas laterais Windows\xa0e Ldap.', 'A configuração é simples, mas requer alguns cuidados para não gerar erros ou falhas no sincronismo na floresta do seu servidor Windows AD.', 'Abaixo vamos especificar alguns campos:Authentication - Windows Server', 'Caso os campos estejam indisponíveis, habilite-os através da opção\xa0[] localizada na frente das abas laterais à esquerda.', 'LDAP Server: Selecionar o perfil ldap que foi criado\xa0através do botão\xa0[] nesta mesma tela. Para mais informações, cheque a página sobre servidor Windows ou a página sobre servidor LDAP. Ex.: Primary DC;Workgroup: Define o workgroup do controlador de domínio. Ex.: DOMAINE;Domain: Define o domínio que será importado os usuários. Ex.: domaine.com.Authentication - Windows ServerPreencha os campos e clique em [] para salvar as configurações.Após salvar, ao clicar no botão [] e clicar\xa0em “Synchronize” é possível realizar o sincronismo de forma manual (para mais informações, vide Windows Server - Sync Interval.Authentication - Want to run the sync now?Após clicar no botão [] o sistema irá iniciar o sincronismo com o servidor remoto.Após confirmar, será necessário acessar a\xa0fila de comandos\xa0[]\xa0e aplicar para o sincronismo acontecer. Para mais informações a respeito da fila de comandos acesse a página:\xa0UTM - Fila de comandos.', 'O princípio de configuração do sincronismo de uma base LDAP é o mesmo. No entanto é importante considerar que as configurações de filtros e base de busca em um servidor LDAP são criadas por quem implementa o serviço de diretório e é necessário ter essas informações para obter êxito na configuração.', '', '']
